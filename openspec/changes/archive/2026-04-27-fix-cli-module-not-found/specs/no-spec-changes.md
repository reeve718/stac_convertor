## No Spec Changes

This is a bug fix that does not introduce new capabilities or modify existing requirements.

The change only corrects the CLI script entry point in `pyproject.toml` to use the correct module path.
